import { Color, Component } from "cc";

//每个模块只能有一个默认导出，导入时不需要大括号
class Obstacle extends Component {
    public setColors(primaryColor: Color, optionalColors: Color[], optionalCount: number): void {

    }
}

export default Obstacle;